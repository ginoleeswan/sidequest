import { CompactWebShell } from '@/components/CompactWebShell';

/** You, the legal pages and the tools keep the phone's tab bar under them. */
export default function PagesLayoutWeb() {
  return <CompactWebShell />;
}
