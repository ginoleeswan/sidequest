import { CompactWebShell } from '@/components/CompactWebShell';

/** A studio page keeps the phone's tab bar under it - see the shell. */
export default function ByLayoutWeb() {
  return <CompactWebShell />;
}
