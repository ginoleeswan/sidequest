import { CompactWebShell } from '@/components/CompactWebShell';

/** A game page keeps the phone's tab bar under it - see the shell. */
export default function GameLayoutWeb() {
  return <CompactWebShell />;
}
