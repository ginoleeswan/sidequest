# RAWG API key — https://rawg.io/apidocs
# NOTE: EXPO_PUBLIC_ vars are embedded in the client bundle. RAWG keys are
# rate-limit identifiers, not secrets, so this is acceptable for RAWG only.
EXPO_PUBLIC_RAWG_API_KEY=your-key-here
